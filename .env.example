# ISOBEL Environment Configuration
# Copy this file to .env and fill in your values

# ============================================
# REQUIRED: Core Configuration
# ============================================
DISCORD_TOKEN=your-discord-bot-token-here
SONGBIRD_BASE_URL=https://your-songbird-api-url
SONGBIRD_API_KEY=your-songbird-api-key
DATABASE_URL=postgresql://user:password@host:5432/isobel?sslmode=require

# Examples for DATABASE_URL:
# Local PostgreSQL:  postgresql://postgres:password@localhost:5432/isobel
# Neon:             postgresql://user:pass@ep-cool-darkness-123456.us-east-2.aws.neon.tech:5432/isobel?sslmode=require
# Supabase:         postgresql://postgres:pass@db.projectref.supabase.co:5432/postgres?sslmode=require

# ============================================
# OPTIONAL: Bot Configuration
# ============================================
DATA_DIR=./data
CACHE_LIMIT=2GB
HEALTH_PORT=3002

# Bot Presence
BOT_STATUS=online
# Options: online, idle, dnd

BOT_ACTIVITY_TYPE=LISTENING
# Options: PLAYING, LISTENING, WATCHING, STREAMING

BOT_ACTIVITY=music
# The text that appears after the activity type

# BOT_ACTIVITY_URL=
# Required ONLY if BOT_ACTIVITY_TYPE=STREAMING
# Must be a Twitch or YouTube URL

# SponsorBlock Integration (skips non-music segments)
# ENABLE_SPONSORBLOCK=false
# SPONSORBLOCK_TIMEOUT=5

# ============================================
# OPTIONAL: Advanced Configuration
# ============================================
# SONGBIRD_NEXT_URL=
# Alternative Songbird API URL (for testing/migration)

# FFMPEG_PATH=/usr/bin/ffmpeg
# FFPROBE_PATH=/usr/bin/ffprobe
# Optional explicit binary paths (only needed if ffmpeg/ffprobe are not in PATH)

# REGISTER_COMMANDS_ON_BOT=false
# Set to true to register commands globally instead of per-guild

# NODE_ENV=production
# Usually set automatically by PM2/Docker (development, staging, production)

# ============================================
# OPTIONAL: Web Interface Configuration
# (Only needed if you want to use the web UI)
# ============================================

# Discord OAuth Application (for web login)
# Create at: https://discord.com/developers/applications
# DISCORD_CLIENT_ID=your-oauth-client-id
# DISCORD_CLIENT_SECRET=your-oauth-client-secret

# NextAuth Configuration (for web authentication)
# Generate a random secret: openssl rand -base64 32
# NEXTAUTH_SECRET=your-random-secret-here
# NEXTAUTH_URL=http://localhost:3001

# Web Server Ports
# WEB_PORT=3001        # Web interface
# AUTH_PORT=3003       # Authentication server
# Note: HEALTH_PORT (3002) is for bot health checks, separate from web services

# ============================================
# DOCKER ONLY: Build Arguments
# ============================================
# These are typically set by docker-compose.yml
# COMMIT_HASH=local
# BUILD_DATE=now
