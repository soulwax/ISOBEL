# Prisma 7 generates a client under node_modules/.prisma that resolves some
# runtime packages from the project root. With pnpm's isolated linker, those
# packages are otherwise not publicly linked, which breaks PM2 and Docker runs.
public-hoist-pattern[]=@prisma/*
